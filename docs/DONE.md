### Среда холодовой цепи
Построить ядро в `core/`: динамика температуры, порча фруктов, граф маршрутов, шумы, конфиг.

### Пакет среды и адаптер
Собрать `env/` с `training_env.py`, reward shaping внутри среды; адаптер под PettingZoo.

### Temperature-агент (DDPG)
Обучить контроль температуры рефрижераторов. Чекпоинт в `artifacts/modules/temperature/`.

### Routing-агент (DQN)
Обучить выбор маршрутов. Чекпоинт в `artifacts/modules/routing/`.

### Spoilage-агент (GNN + DDPG)
Обучить предсказание порчи по графу сети, отдельный pretrain энкодера. Чекпоинты в `artifacts/modules/spoilage*/`.

### Inventory-агент (DDPG)
Обучить управление запасами. Чекпоинт в `artifacts/modules/inventory/`.

### Delivery-агенты (MADDPG, ×3)
Обучить координацию трёх машин доставки. Чекпоинты в `artifacts/modules/delivery_*/`.

### Динамические Парето-веса ω
Добавить балансировку конфликтующих целей (скорость / энергия / порча) в общий reward.

### Intention buffer (ρ)
Добавить обмен намерениями между агентами — каждый учитывает планы остальных.

### Полный цикл обучения CTDE
Прогнать 150 итераций на TorchRL, все агенты сошлись. Кривые в `artifacts/reward_curve.csv`.

### Оценка против baseline
Сравнить с random baseline: temperature +98, spoilage +81, inventory +76, routing +60, delivery +43.

### Ноутбуки
Собрать `dataset_report`, `training_report`, `agent_behavior`.

### Рефакторинг
Вынести MADDPG в отдельный модуль, удалить мёртвое состояние, среду собрать в `env/`.

### Сверка с Алг. 1–5 и подготовка к фазе GenAI
Пройти все пять алгоритмов построчно, исправить найденное: веса α входили в формулу динамических весов в квадрате; inventory видел истинное среднее генератора спроса (утечка, наблюдения 5 → 4, поле переименовано в `demand_forecast`). Зафиксировать дизайн GenAI: мультипликативный генератор спроса с календарём и событиями-пиками, трансформер учится офлайн, работает онлайн через буфер истории продаж.
### Генератор спроса
Мультипликативная модель: base × сезон (±30%) × день недели (×1.2 weekend) × погода × событие (p=0.02, 1–3 дня, ×1.5–2.5) × логнормальный шум σ=0.1. Календарь тик=день, `core/world/demand.py`.

### Датасет для трансформера
50 рядов × 2000 дней напрямую из генератора, `data/generate_demand.py` → `data/demand/` (npz + manifest с seeds/sha256/снапшотом конфига). Сплит по времени 70/15/15.

### Трансформер-форкастер
TransformerEncoder 2 слоя, d_model 64, ~69k параметров; окно 28 дней × 10 фич + токен target-дня. `training/forecaster/`, чекпоинт `artifacts/modules/forecaster/`.

### Оценка прогноза
MAE 0.134 vs 0.347 last-value vs 0.364 seasonal-naive, oracle floor 0.099 → извлечено 85.8% предсказуемого. Событийные дни: 0.83 vs 0.89–1.19 (старты непредсказуемы by design).

### Буфер истории и прогноз в среду
GlobalState держит окно 28 дней (backfill при reset, события переживают границу); `config["forecaster"]` → замороженный трансформер заполняет `demand_forecast` каждый шаг (MAE к завтрашнему спросу 0.022 vs 0.051 у заглушки).

### Дообучение inventory и delivery
Inventory с нуля: cost 0.209 vs 0.526 random (+60%). Абляция заглушка-vs-трансформер: Δreturn ≤ 0.005 — reward Алг. 4 спрос не штрафует, прогноз декоративен (зафиксировано, reward не меняем). Загадка «cost 0.35 → 0.62» решена: смена шкалы после фикса Pareto.

### Регресс-прогон чекпоинтов
Устарел заменой: после фиксов Pareto/метрик все пять агентов переобучены вместе на новой среде — temperature +98%, routing return 95.0 vs 0.6, spoilage +63%, inventory +59%, delivery −63% по slot_cost (не обучаем до Phase W, см. status 2026-07-14).

### LLM-клиент и банк сценариев сбоев
Провайдер-независимый клиент (`llm/client.py`, любой OpenAI-совместимый эндпоинт, structured output + retry; ответ reasoning-моделей в `reasoning_content` обработан). Схема сценария: 7 категорий × 6 эффектов `{kind, magnitude, target_role}`, диапазоны проверяет валидатор (грамматика их не гарантирует). Банк: 66 сценариев от Nemotron 3 Nano 4B (67 отбраковано, 0 дубликатов), `data/scenarios/bank.json`; реплей в эпизодах через `env/scenarios.py` (`scenario_bank`/`scenario_prob`/`scenario_id`).

### Стресс-прогон по банку сбоев
`training/marl/stress_eval.py`: метрики блоков на чистых эпизодах vs каждая категория (seed 700 000). Деградация согласована с физикой: temperature — термальные категории (cold snap +15%), spoilage — power outage (+68% FN), routing — labor strike (+4%), delivery инертен.

### Дообучение на сценариях: доза решает
prob=1.0 (рецепт статьи в лоб) — catastrophic forgetting у temperature (clean 0.207→0.328), но закрывает дыру routing по labor strike (+4%→0%); prob=0.5 — регуляризатор (temperature 0.158, spoilage 0.068 clean — лучше baseline даже на чистых). Рабочий набор: `temperature_scn05`, `spoilage_scn05`, `routing_scn`, inventory/delivery baseline.

### LLM-переговоры (Алг. 6)
`llm/negotiation.py` — протокол по букве бокса: последовательные counter-offers (уступка только если переезд дешевле конфликтного штрафа), S_t = L(H) с win-win предложением, agreement = сами оферты, U против порогов τ = статус-кво с конфликтом; n-party обобщение (конфликт бывает 3-сторонним). Прогон `training/marl/negotiation_eval.py` (seed 800 000, 30 эпизодов): conflict 0.667→0.000, slot_cost 3.659→0.114, SLA −67%; 464/464 соглашений за 1 раунд, LLM-арм = greedy-арм. Находка репродукции: медиатор-суммаризатор из бокса оставляет остаток 0.114 против 0.013 у медиатора-диктатора — позиционная уступка фиксирует первую приемлемую сделку раньше, чем S_t успевает повлиять. Alg 7 — блокчейн-фаза.

### Реальный retail-датасет — эксперимент, в проект не включён
Статья полностью синтетическая — реальный датасет репродукции не нужен. Разовый прогон на M5 (FOODS, 30 рядов dept×store): трансформер MAE 0.154 vs 0.223 last-value (−31%), архитектура подтверждена. Калибровка снята: weekend 1.27 (у нас 1.2), сезонность 0.059 (0.3), шум σ 0.38 (0.1). Код удалён, константы генератора не меняли.
