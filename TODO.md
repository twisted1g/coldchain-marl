### Сезонность и погода в генераторе спроса

Добавить в спрос годовую/недельную сезонность и погодный фактор через `inventory_rng`. Воспроизводимость по seed не ломать.

### Регресс-прогон чекпоинтов на новой среде

Прогнать текущие чекпоинты на обогащённом спросе, обновить `dataset_report.ipynb`.

### Датасет продаж
Сгенерировать исторические продажи.

### Трансформер
Обучить модель прогноза спроса по спецификации статьи. Скрипт в `training/`, чекпоинт в `artifacts/modules/forecaster/`.

### Бейзлайны прогноза
Посчитать MAE/MAPE трансформера на test против last value и seasonal naive.

### Прогноз в наблюдения агентов
Заменить заглушку `predicted_demand` у inventory на выход трансформера; прогноз — входом F в MADDPG (Алг. 5).


### Дообучение inventory и delivery с прогнозом
Дообучить обоих, сравнить метрики с текущими (+76 / +43). Разобраться, почему inventory cost вырос 0.35 → 0.62 при улучшении return.


### Банк сценариев сбоев
Подключить LLM для генерации JSON-сценариев сбоев (disruption engine): отказ холодильника, закрытый маршрут, скачок спроса.
Добавить в среду через события.


### Стресс-прогон по банку сбоев
Прогнать обученных агентов по всем сценариям, записать деградацию метрик по каждому типу сбоя.
